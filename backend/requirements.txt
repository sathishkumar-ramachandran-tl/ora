# Core Framework
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.5
Flask-Cors==4.0.0
Flask-JWT-Extended==4.5.3
gunicorn==21.2.0
python-dotenv==1.0.0
Flask-Mail==0.9.1

# Database
psycopg[binary]

# Google AI — must use >=1.0.0; old 0.x is incompatible with google-adk and langchain-google-genai
google-genai>=1.0.0

# Google APIs
google-api-python-client
google-auth-httplib2
google-auth-oauthlib

# === AGENTIC AI STACK ===

# LangGraph — multi-agent orchestration + in-memory checkpointing
langgraph>=0.2.0
langgraph-checkpoint>=1.0.0
langgraph-prebuilt>=0.1.0

# LangChain — tool definitions and model integration
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0

# MCP — Model Context Protocol server (stdio transport)
mcp>=1.0.0

# Google ADK — Agent Development Kit
google-adk>=0.5.0

# Async / HTTP utilities
anyio>=4.0.0
httpx>=0.27.0
